<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>{{name}} - CV</title>
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; margin: 40px; }
    h1 { color: #2c3e50; border-bottom: 2px solid #3498db; }
    .section { margin-bottom: 20px; }
  </style>
</head>
<body>
  <h1>{{name}}</h1>
  <div class="section">
    <h2>Información de Contacto</h2>
    <p>Email: {{email}}</p>
    <p>Teléfono: {{phone}}</p>
  </div>
  <div class="section">
    <h2>Experiencia Laboral</h2>
    <ul>
      {{#each experience}}
      <li><strong>{{title}}</strong> en {{company}} ({{period}})</li>
      {{/each}}
    </ul>
  </div>
</body>
</html>